name: testFlow
flow:
  inputs:
    nextScreens:
      - name: housemateInfo
  housemateInfo:
    group: household
    nextScreens:
      - name: householdList
  householdList:
    nextScreens:
      - name: success
  success:
    nextScreens: